# ☁️ Real-time Word Cloud Service

Интерактивный сервис для создания облака слов в реальном времени. Идеально подходит для конференций, лекций и мероприятий. Участники отвечают на вопросы через свои смартфоны, а результаты мгновенно появляются на общем экране в виде красивого облака тегов.

![Presentation View](https://via.placeholder.com/800x450.png?text=Word+Cloud+Presentation)

## ✨ Возможности

- **Реактивность:** Ответы появляются на экране презентации мгновенно (Server-Sent Events).
- **Красивая визуализация:** Слова автоматически распределяются, меняют размер в зависимости от популярности и окрашиваются в яркие цвета.
- **Умная обработка:** Сохраняет фразы целиком (например, "не знаю"), фильтрует нецензурную лексику (базовая) и стоп-слова.
- **Темы:** Поддержка светлой и темной темы для разных условий освещения (проектор/экран).
- **Админ-панель:** Полный контроль над опросом (старт/стоп/сброс).
- **QR-код:** Автоматическая генерация QR-кода для быстрого входа участников.
- **Адаптивный дизайн:** Отлично выглядит на мобильных устройствах и больших экранах.

## 🛠 Технологический стек

- **Frontend/Backend:** Next.js 16 (App Router)
- **Database:** SQLite + Prisma ORM
- **Visualization:** D3-cloud
- **Real-time:** Native EventSource (SSE)
- **Styling:** Tailwind CSS + Glassmorphism UI
- **Testing:** Jest + Visual Verification Scripts

---

## 🚀 Как запустить локально (для разработки)

**Требования:** Node.js 20+ (рекомендуется 22, см. `.nvmrc`).

1. **Клонируйте репозиторий:**
   ```bash
   git clone <your-repo-url>
   cd words-cloud-project
   ```

2. **Настройте окружение:**
   ```bash
   # Установить правильную версию Node.js
   nvm use
   
   # Установите зависимости
   npm install
   ```

3. **Подготовьте базу данных:**
   ```bash
   # Создает локальную SQLite базу данных и применяет миграции
   npx prisma migrate dev --name init
   ```

4. **Запустите сервер разработки:**
   ```bash
   npm run dev
   ```

5. **Откройте в браузере:**
   - **Админка:** [http://localhost:3000/admin](http://localhost:3000/admin) (Ключ: `admin123`)
   - **Презентация:** [http://localhost:3000/presentation](http://localhost:3000/presentation)
   - **Опрос (для участников):** [http://localhost:3000/survey](http://localhost:3000/survey)

---

## 🧪 Тестирование и Разработка

### 1. Автоматические тесты (Unit)
Запуск юнит-тестов для проверки логики разбиения слов и фильтрации:
```bash
npm test
```

### 2. Заполнение базы тестовыми данными (Seeding)
Если нужно заполнить облако 150 случайными словами для проверки производительности:
```bash
npx prisma db seed
```

### 3. Визуальная проверка (Visual Test)
Скрипт добавляет уникальное слово 5 раз подряд с задержкой, чтобы вы могли визуально проверить обновление облака на экране презентации:
```bash
npx tsx scripts/visual-test.ts
```
*Запустите этот скрипт в отдельном терминале, пока открыта страница презентации.*

---

## 🐳 Запуск через Docker (Production)

Для запуска на сервере используется Docker Compose.

1. **Соберите и запустите контейнер:**
   ```bash
   docker-compose up -d --build
   ```

2. **Настройка переменных окружения:**
   В файле `docker-compose.yml` можно настроить:
   - `ADMIN_SECRET`: Пароль для входа в админку.
   - `NEXT_PUBLIC_APP_URL`: Ваш домен (например, `https://mysite.com`), используется для генерации правильного QR-кода.

---

## 📱 Руководство пользователя

### 1. Администратор (Ведущий)
1. Зайдите на `/admin`.
2. Введите секретный ключ: `admin123` (по умолчанию).
3. Введите вопрос для аудитории.
4. Нажмите **"Запустить опрос"**.
5. Откройте ссылки на **QR-код** (чтобы показать людям) и **Презентацию** (вывести на проектор).
6. Используйте переключатель ☀️/🌙 в презентации для настройки под освещение.

### 2. Участник
1. Сканирует QR-код.
2. Вводит свое имя.
3. Пишет ответ.
4. Видит свой ответ в общем облаке.

### 3. Сброс результатов
Чтобы начать новый опрос и очистить облако:
1. В админке нажмите **"Завершить опрос"**.
2. Введите новый вопрос.
3. Нажмите **"Запустить опрос"** снова.

---

## 📁 Структура проекта

- `/app` - Основной код приложения (Next.js App Router).
  - `/admin` - Страница управления.
  - `/presentation` - Экран с облаком слов.
  - `/survey` - Интерфейс участника.
  - `/api` - API эндпоинты (SSE stream, polling, save response).
- `/prisma` - Схема базы данных SQLite и сид-скрипты.
- `/components` - React компоненты (Cloud.tsx и др).
- `/scripts` - Скрипты для тестирования и обслуживания.
- `/__tests__` - Юнит-тесты Jest.
