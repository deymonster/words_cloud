version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ADMIN_SECRET=${ADMIN_SECRET}
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - ./data:/app/data

